server.port=8081

# Tomcat thread pool - default is 200, we set to 10 to show thread exhaustion faster
server.tomcat.threads.max=10

spring.datasource.url=jdbc:h2:mem:blockingdb
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
